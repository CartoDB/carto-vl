# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true

# Javascript and JSON files are formatted with 4 spaces with ut8 encoding
[*.{js,json}]
charset = utf-8
indent_style = space
indent_size = 4
